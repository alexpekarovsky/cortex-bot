openapi: 3.0.0
paths:
  /public_api/v1/assets/:
    post:
      summary: Get All or Filtered Assets
      description: |-
        Retrieve a list of all your assets or a filtered list based on specified criteria.
        The maximum result limit is 5000 assets per request.
        A POST request is used to allow for a more complex filtering body.
      operationId: get_assets
      tags:
        - Assets
      requestBody:
        description: JSON object containing filters, sorting, and pagination settings. An empty request_data object returns all results up to the limit.
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssetRequest'
            examples:
              filtered_request:
                summary: Example request with filters and sorting
                value:
                  request_data:
                    filters:
                      - field: "name"
                        operator: "in"
                        value: "AsmAssetsFilter_value"
                    sort:
                      - FIELD: "xdm.asset.name"
                        ORDER: "DESC"
                    search_from: 0
                    search_to: 100
              on_demand_fields_request:
                summary: Example with on-demand fields
                value:
                  request_data:
                    filters: []
                    on_demand_fields:
                      - "xdm.host.ipv4_addresses"
                    search_to: 50
      responses:
        '200':
          description: Successful response containing a list of assets.
          content:
            application/json:
              schema:
                type: object
        '401':
          description: Unauthorized. The API key is missing or invalid.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: "Unauthorized"
        '403':
          description: Forbidden. The API key does not have the required permissions.
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: "Forbidden"
components:
  schemas:
    AssetRequest:
      type: object
      properties:
        request_data:
          $ref: '#/components/schemas/AssetRequestPayload'
    AssetRequestPayload:
      type: object
      properties:
        filters:
          type: array
          items:
            $ref: '#/components/schemas/Filter'
        on_demand_fields:
          type: array
          items:
            type: string
        sort:
          type: array
          items:
            $ref: '#/components/schemas/SortItem'
        search_from:
          type: integer
          default: 0
        search_to:
          type: integer
          default: 1000
    Filter:
      type: object
      properties:
        field:
          type: string
        operator:
          type: string
          enum:
            - in
            - gte
            - lte
            - contains
            - like
        value:
          type: string
    SortItem:
      type: object
      properties:
        FIELD:
          type: string
        ORDER:
          type: string
          enum:
            - ASC
            - DESC
